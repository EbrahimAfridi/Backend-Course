<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style/styles.css" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <nav class="mb-10 text-2xl flex gap-5">
        <a href="/read" class="hover:underline underline-offset-4">All Users</a>
        <a href="/edit" class="hover:underline underline-offset-4">Edit User</a>
        <a href="/delete" class="hover:underline underline-offset-4"
          >Delete User</a
        >
      </nav>

      <h1 class="text-3xl">Update User</h1>
      <hr class="mt-2 mb-4 bg-zinc-600 h-px" />
      <form action="/update/<%= user._id %>" method="post">
        <input
          type="text"
          class="px-5 py-3 bg-transparent border-2 border-zinc-800 rounded outline-none"
          placeholder="name"
          value="<%= user.name %>"
          name="name"
        />
        <input
          type="email"
          class="px-5 py-3 w-min bg-transparent border-2 border-zinc-800 rounded outline-none"
          placeholder="email"
          value="<%= user.email %>"
          name="email"
        />
        <input
          type="url"
          class="px-5 py-3 bg-transparent border-2 border-zinc-800 rounded outline-none"
          placeholder="image url"
          value="<%= user.imageUrl %>"
          name="imageUrl"
        />
        <input
          class="ml-3 px-5 py-3 rounded-md outline-none bg-yellow-500 text-black cursor-pointer hover:bg-yellow-600"
          type="submit"
          value="Update User"
        />
      </form>
    </div>
  </body>
</html>
