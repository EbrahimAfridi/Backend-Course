<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>

  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="">
        <h3 class="text-3xl">Profile</h3>
        <a
          class="px-3 py-1 mt-4 bg-red-500 rounded-md font-semibold text-lg block w-fit"
          href="/logout"
          >Logout</a
        >
      </div>
      <hr class="mt-2 mb-5" />
      <div class="flex flex-row w-fit py-2 text-2xl gap-10 mb-10">
        <h4><%= user.name %></h4>
        <h4><%= user.email %></h4>
      </div>
      <p>you can create a new post.</p>
      <form action="/post" method="post">
        <textarea
          placeholder="What's on your mind?"
          name="content"
          id="content"
          class="mt-2 placeholder:italic placeholder:text-white block outline-none resize-none w-1/3 bg-zinc-800 p-3 border-2 border-zinc-800 rounded-md"
        ></textarea>
        <input
          value="Create new post"
          type="submit"
          class="block px-5 py-2 w-40 bg-blue-600 hover:bg-blue-700 hover:cursor-pointer font-semibold mt-4 rounded-md"
        />
      </form>
      <div class="mt-20">
        <h3 class="text-zinc-400">Your Posts</h3>
        <% user.posts.reverse().forEach((post) => { %>
        <div class="mt-4">
          <div
            class="w-1/3 p-4 border-2 border-zinc-800 bg-zinc-800 rounded-md"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.name %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <div class="flex mt-5">
              <a href="/likes/<%= post._id %>" class="text-blue-500 hover:underline">Like</a>
              <a href="#" class="text-zinc-400 hover:underline ml-2">Edit</a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>